{
  "name": "create-graphql-service",
  "version": "0.0.1",
  "description": "create a GraphQL microservice with auth0 authentication",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Marijnissen",
  "license": "MIT",
  "peerDependencies": {
    "apollo-server-micro": "^1.0.4",
    "graphql": "^0.10.1",
    "micro": "^8.0.0"
  },
  "dependencies": {
    "eventemitter3": "^2.0.3"
  }
}
